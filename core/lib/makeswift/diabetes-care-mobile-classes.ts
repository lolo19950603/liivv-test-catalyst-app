/** Apply to archive `.slider--tablet` wrappers that should stack vertically on phones/tablets. */
export const DC_MOBILE_STACK_CLASS = 'dc-mobile-stack';

/** Apply to horizontal swipe rows (products, collections, timeline). */
export const DC_MOBILE_CAROUSEL_CLASS = 'dc-mobile-carousel';

/** Parent of `.dc-mobile-carousel` — neutralizes archive `.slider--tablet` bleed on small screens. */
export const DC_CAROUSEL_HOST_CLASS = 'dc-carousel-host';

/** Root wrapper for all specialized-page sections (clip X without forcing Y scroll). */
export const DC_SECTION_ROOT_CLASS = 'dc-section-root';
